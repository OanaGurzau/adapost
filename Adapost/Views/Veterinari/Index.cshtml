@model IEnumerable<Adapost.Models.Veterinari>

@{
    ViewBag.Title = "Index";
}

<h2>Veterinari</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.IdVeterinar)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Nume)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Prenume)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.IdVeterinar)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Nume)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Prenume)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdVeterinar }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdVeterinar }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdVeterinar })
        </td>
    </tr>
}

</table>
